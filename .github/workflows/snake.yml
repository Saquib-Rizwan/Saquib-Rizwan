name: Generate contribution snake

on:
  schedule:
    # Rebuilds the animation once a day so it stays current.
    - cron: "0 2 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Generate snake from contribution graph
        uses: Platane/snk@v3
        id: snake
        with:
          github_user_name: Saquib-Rizwan
          outputs: |
            dist/snake.svg?color_snake=%23F59E0B&color_dots=%23e2e8f0,%23fcd34d,%23fbbf24,%23f59e0b,%23d97706
            dist/snake-dark.svg?color_snake=%23F59E0B&color_dots=%231e293b,%23fcd34d,%23fbbf24,%23f59e0b,%23d97706

      # Publishes to an "output" branch, which is what README.md links to.
      # Keeping it off main means generated files never clutter your history.
      - name: Publish to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
